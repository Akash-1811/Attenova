# Minimal config for ruff (used in CI and optionally locally)
[tool.ruff]
target-version = "py312"
line-length = 120
exclude = ["migrations", ".git", "__pycache__", "venv", ".venv"]

[tool.ruff.format]
quote-style = "double"
